# Plan docs

This folder holds temporary per-issue or per-PR planning context.

Plan folders may be deleted when the related PR is merged, closed, or abandoned. Before deleting a plan folder, promote any durable decisions or verified findings into `docs/`.

References from files under `dev-docs/plan/**` are not compatibility constraints for durable documentation moves. Prefer updating or deleting the plan folder as part of PR cleanup.
